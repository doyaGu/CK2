@PACKAGE_INIT@

# Include the exported targets
include("${CMAKE_CURRENT_LIST_DIR}/CK2Targets.cmake")

# Find dependencies (only if not bundled in CK2Targets)
if(NOT TARGET VxMath AND NOT TARGET VxMathStatic)
	include(CMakeFindDependencyMacro)
	find_dependency(VxMath REQUIRED)
endif()

check_required_components(CK2)
